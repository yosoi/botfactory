# TODO: create custom lambda resolver mutations for this type
# TODO: get bot name from lambda field resolver via discord api using clientId
# TODO: set instance id in custom create mutation after instancing an image
# TODO: get instance state via lambda field resolver via ec2 using instanceId
# TODO: set create and update mutations = null
type Bot
  @auth(rules: [{ allow: owner }, { allow: groups, groups: ["Admin"] }])
  @model {
  id: ID!
  name: String!
  clientId: ID!
  clientSecret: ID!
  token: ID!
  instanceId: ID!
  instanceState: InstanceState!
  prefix: String!
}

enum InstanceState {
  PENDING
  RUNNING
  SHUTTING_DOWN
  TERMINATED
  STOPPING
  STOPPED
}
